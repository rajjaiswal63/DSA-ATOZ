#include<iostream>
using namespace std;
int main(){
    int a,b;
    cout<<"Enter two no. to compare which is greater : "<<endl;
    cin>>a>>b;
    if(a>b) cout<<a<<"is greater then b ";
    else cout<<b<<"is greater then a ";
}